The input is already a finished Marp deck. Keep it. Your only job is to make
the slides that clip fit the page.

Rules for this pass, on top of the standing ones:

- Change nothing on a slide that was not named as clipping.
- On a slide that clips, cut until it is at most 12 lines of content: drop the
  weakest bullet, shorten a bullet to one line, or split the slide in two at a
  natural break and give the second half the same heading with a `(cont.)`
  suffix.
- Keep every heading, every `<!-- _class: ... -->` comment, every front-matter
  line, and every fence exactly as they are.
- Cut whole bullets rather than rewording surviving ones. A sentence that stays
  stays verbatim.
- Never drop a number, a date, a name, or a URL. If a bullet is the only place
  one appears, shorten the words around it instead.

Answer with the whole deck, every slide, not just the ones you changed.
